
  # Login Logout Page

  This is a code bundle for the Graveyard Management System.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  